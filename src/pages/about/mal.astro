---
// The standalone "Mal" page has been merged into /about (see the "Team"
// section, id="team", in about/index.astro) as part of the site's
// architecture streamlining — one less nav item, one less page to keep in
// sync. This redirect preserves old bookmarks/links/search results.
return Astro.redirect('/about#team', 301);
---
