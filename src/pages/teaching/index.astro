---
// /teaching is retired — its interactive tools list moved to /tools
// (alongside the projects collection, since both are the same category
// of thing: apps Diorama has built), and its "from the blog" teaser was
// redundant with /insights. See the conversation that led here.
return Astro.redirect('/tools/', 301);
---
